Note. We have removed the unit tests of the project. Our project
contains only the integration tests displayed in the previous code listings.

To run the tests

[source, bash]
----
./grailsw
grails> test-app
grails> open test-report
----
